spring:
  application:
    name: Learning Log
  profiles:
    group:
      default:
        active: dev
        include: dev,test,prod
    active: dev
  datasource:
    name: learning_log
    url: jdbc:h2:mem:learning_log
    username: root
    password: root
  sql:
    init:
      mode: always
  thymeleaf:
    cache: off
logging:
  level:
    root: info
